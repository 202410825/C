#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <math.h>
double pow(double* a)
{
	return pow(a, 2);
}
double sqrt(double x)
{
	return sqrt(x);
}
int main(void)
{
	int* arr[5];
	double total = 0;
	for (int i = 0; i < 5; i++) {
		scamf("%d", arr[i]);
	}
	for (int i = 0; i < 5; i++) {
		total += 
	}


	return 0;
}


